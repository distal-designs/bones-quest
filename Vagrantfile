Vagrant.configure("2") do |config|
  config.vm.box = "nafarlee/windows-10-pro-64bit"

  # Set-ExecutionPolicy RemoteSigned -scope CurrentUser
  # iex (new-object net.webclient).downloadstring('https://get.scoop.sh')
  # scoop install rust git gcc
  # git clone https://github.com/distal-designs/bones-quest
  # cd bones-quest
  # cargo run
end
